import trufDetails from "../components/layout/turf_details";
import { connect } from "react-redux";

const mapStateToProps = (state) => {};

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(trufDetails);
